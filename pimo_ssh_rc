#!/bin/sh
# PiMO SSH splash
if [ -t 1 ] && [ -n "$SSH_CONNECTION" ] && [ -x "$HOME/.local/bin/pimo_splash.py" ] && [ -z "$PIMO_SPLASH_SHOWN" ]; then
  PIMO_SPLASH_SHOWN=1 "$HOME/.local/bin/pimo_splash.py" || true
fi
