# /etc/pimo/rss2discord.conf
# PiMO RSS → Discord relay configuration

# REQUIRED: Your Discord webhook URL
WEBHOOK_URL=https://discord.com/api/webhooks/.../...

# Comma/space/newline separated list of feeds
FEEDS=https://hnrss.org/frontpage, https://xkcd.com/atom.xml

# Optional tuning
MAX_PER_RUN=5
TIMEOUT_SECONDS=5
USER_AGENT=pimo-rss2discord/1.0 (+https://pimo)
MAX_CONCURRENCY=8
# META_PATH=/var/lib/pimo/rss2discord.meta.json
FETCH_BUDGET_SECONDS=10
VERBOSE=1

# Dynamic feeds sources (optional)
# Point to a newline-delimited file or a directory of such files
# Lines may contain comments with # and shortcuts like r/subreddit
# FEEDS_FILE=/etc/pimo/feeds.list
# FEEDS_DIR=/etc/pimo/feeds.d/

# Paths (overrides). Defaults are fine on Linux; these can be left unset.
# LOG_PATH=/var/log/pimo/rss2discord.log
# STATE_PATH=/var/lib/pimo/rss2discord.seen

